<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.shape.repository.TestBlobRepository">

	<insert id="save" parameterType="java.util.Map">
		insert into TEST_BLOB values ( #{bytes, jdbcType=BLOB, javaType=[B} )
	</insert>

	<!-- file Upload 예시 -->
	<insert id="insertTest"
		parameterType="com.itwill.shape.domain.Test">
        INSERT INTO test (
              testTitle
            , testContent
            , testName
            , testDate
            , fileName
        ) VALUES (
            #{testTitle}
            ,#{testContent}
            ,'testUser'
            , systimestamp
            ,#{fileName}
        )
	</insert>

</mapper>